apiVersion: v1
kind: Service
metadata:
  name: posts-service
  labels:
    env: dev 
spec:
  selector:
    app: posts-pod  # Utilisez le label du pod que vous souhaitez exposer
  ports:
    - protocol: TCP
      port: 80  # Port du service NodePort
      targetPort: 6000  # Port du conteneur dans le pod
  type: NodePort
